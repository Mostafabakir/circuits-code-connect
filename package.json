{
  "name": "vite_react_shadcn_ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages --dist dist --branch gh-pages --dotfiles"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}